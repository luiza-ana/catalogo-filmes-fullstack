export default class Filme {
  constructor(id, titulo, genero, ano, descricao) {
    this.id = id;
    this.titulo = titulo;
    this.genero = genero;
    this.ano = ano;
    this.descricao = descricao;
  }
}
